<h2>My Tasks</h2>
<!-- <ul> -->
  <% @tasks.each do |task| %>
    <!-- <input type="checkbox"> -->
    <!-- <li><%= link_to task.title, task_path(task) %></li> -->
    <%= form_for(task) do |f| %>
      <div class="field">
        <%= f.check_box :completed %>
  <!--  <%= f.label task.title %>
   -->  <%= link_to task.title, task_path(task) %>
        <%= link_to "🗑",
              task_path(task),
              method: :delete,
              data: { confirm: "Are you sure?" } %>
        <% end %>
      </div>
  <% end %>
<!-- </ul> -->


<p><%= link_to "Add Task", new_path %></p>
